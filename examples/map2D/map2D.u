
void putint(int i);
void putendl();
void putaddr(int x[]);

int lol(int x[2])
{
	putint(x[0]); putendl();
	putint(x[1]); putendl();
	return 0;
}

void main()
{
	int i, j, x[10][2];
	
	for (i = 0; i < 10; ++i)
	{
		for (j = 0; j < 2; ++j)
		{
			x[i][j] = 5*i+j;
		}
	}

	map(lol, x);
}
