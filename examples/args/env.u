void putint(int c);
int puts(string s);

int main(int argc, string argv[], string envp[])
{
    int i;

    argc;  // évite les warning de non utilisation
    argv;  // remarque: pas de warning pour le fait que
           // ces instructions ne servent à rien

    for (i = 0; (int)(envp[i]) != 0; ++i)
    	puts(envp[i]);

    return 0;
}
    
