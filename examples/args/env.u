void putint(int c);
int puts(string s);

int main(int argc, string argv[], string envp[])
{
    int i;
    putint(argc);
    for (i = 0; i < argc; i++)
    {
        puts(argv[i]);
    }

    for (i = 0; (int)(envp[i]) != 0; ++i)
    	puts(envp[i]);

    return 0;
}
    
